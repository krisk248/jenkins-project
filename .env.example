# ============================================================================
# TTS JENKINS ENVIRONMENT CONFIGURATION
# ============================================================================
# Copy this file to .env and fill in your actual values
# The .env file should NEVER be committed to Git (it's in .gitignore)
#
# Instructions:
# 1. Copy: cp .env.example .env
# 2. Fill in all the values below
# 3. Keep .env file secure - it contains passwords and tokens
# ============================================================================

# ----------------------------------------------------------------------------
# JENKINS ADMIN CONFIGURATION
# ----------------------------------------------------------------------------
# Jenkins administrator credentials
JENKINS_ADMIN_USER=admin
JENKINS_ADMIN_PASSWORD=your-strong-password-here
JENKINS_ADMIN_EMAIL=devops@ttsme.com

# Jenkins server hostname or IP (where Docker is running)
# Example: 192.168.1.136 or build-server.ttsme.com
JENKINS_HOST=192.168.1.136

# ----------------------------------------------------------------------------
# GIT/GITHUB CREDENTIALS
# ----------------------------------------------------------------------------
# GitHub username and Personal Access Token for Git operations
# To create a token: GitHub → Settings → Developer settings → Personal access tokens
# Required scopes: repo (full control)
GITHUB_USERNAME=your-github-username
GITHUB_TOKEN=ghp_your_github_personal_access_token

# ----------------------------------------------------------------------------
# NETWORK SHARE CREDENTIALS
# ----------------------------------------------------------------------------
# Credentials for accessing \\192.168.1.136\tts-builds
NETWORK_SHARE_USER=your-network-username
NETWORK_SHARE_PASS=your-network-password

# ----------------------------------------------------------------------------
# SONARQUBE CONFIGURATION
# ----------------------------------------------------------------------------
# SonarQube authentication token
# Generate from: SonarQube → Administration → Security → Users → Tokens
# Or use: curl -u admin:admin -X POST "http://192.168.1.136:9000/api/user_tokens/generate?name=jenkins"
SONARQUBE_TOKEN=squ_your_sonarqube_token_here

# ----------------------------------------------------------------------------
# EMAIL/SMTP CONFIGURATION
# ----------------------------------------------------------------------------
# SMTP server settings for email notifications
#
# For Gmail:
#   SMTP_SERVER=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USER=your-email@gmail.com
#   SMTP_PASS=your-app-specific-password (not your regular Gmail password!)
#   Generate app password: Google Account → Security → 2-Step Verification → App passwords
#
# For Office 365:
#   SMTP_SERVER=smtp.office365.com
#   SMTP_PORT=587
#   SMTP_USER=your-email@company.com
#   SMTP_PASS=your-password
#
# For custom SMTP:
#   SMTP_SERVER=your-smtp-server.com
#   SMTP_PORT=587 or 465
#   SMTP_USER=smtp-username
#   SMTP_PASS=smtp-password

SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=jenkins@ttsme.com
SMTP_PASS=your-smtp-password-or-app-password
SMTP_REPLY_TO=devops@ttsme.com

# Email addresses for notifications
# DevOps team email (receives all build notifications)
DEVOPS_EMAIL=devops@ttsme.com,kannan@ttsme.com

# ----------------------------------------------------------------------------
# SHARED LIBRARY REPOSITORY
# ----------------------------------------------------------------------------
# Git repository URL for Jenkins Shared Library
# This library contains reusable pipeline functions for builds, security scans, deployments
SHARED_LIBRARY_REPO=https://github.com/krisk248/jenkins-shared-library.git

# ----------------------------------------------------------------------------
# BUILD AGENT CONFIGURATION (for Phase 2)
# ----------------------------------------------------------------------------
# Build agent hostname or IP (same as Jenkins host for now)
# This will be the Ubuntu server where Jenkins agent runs
BUILD_AGENT_HOST=192.168.1.136

# ============================================================================
# NOTES
# ============================================================================
# 1. Never commit .env file to Git - it contains sensitive credentials
# 2. Use strong passwords for JENKINS_ADMIN_PASSWORD
# 3. GitHub token needs 'repo' scope for private repositories
# 4. SMTP password for Gmail requires "App Password" not regular password
# 5. Test email settings after setup: Jenkins → Manage Jenkins → System Configuration → Extended E-mail Notification → Test Configuration
# 6. Generate SonarQube token after SonarQube is running
# ============================================================================

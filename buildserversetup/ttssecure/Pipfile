[source]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
reportlab = ">=4.0.0"
pillow = ">=10.0.0"
pyyaml = ">=6.0.0"
jinja2 = ">=3.1.0"
matplotlib = ">=3.5.0"

[dev-packages]

[requires]
python_version = "3.10"
